name: preflight
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pandas>=1.3
  - numpy>=1.21
  - scipy>=1.7
  - scikit-learn>=1.0
  - pytest>=7.0
  - pytest-cov>=4.0
  - black>=24.0
  - ruff>=0.4
  - pip:
      - -e .
