# Train-Test Drift

Checks split integrity between training and evaluation datasets:

- numeric drift (PSI)
- categorical drift (TVD)
- missingness delta drift

Typical remediation: revisit split strategy and monitor drift in data refresh pipelines.
