# Data Types

Checks type consistency and common ingestion issues:

- numeric values stored as object/string
- mixed Python types inside object columns

Typical remediation: enforce explicit dtype casting in ingestion and schema contracts.
