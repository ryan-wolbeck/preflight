# Completeness

Checks dataset and column-level missingness.

- Warn threshold: configurable (`completeness.warn_threshold`)
- Fail threshold: configurable (`completeness.fail_threshold`)
- Typical remediation: inspect upstream extraction, impute/drop with rationale
